PagerDuty Cherwell ITSM integration repository containing the PagerDuty mApp artifact file.

## Platform Standards

<mandatory_pre_task>
NEVER begin implementation, planning, or code changes without first completing these steps:

1. Read the manifest: `~/.cache/pagerduty/org-standards/docs/MANIFEST.md`
2. Identify files relevant to this repo's stack and the current task.
3. Read the identified files. Paths in the manifest are relative to `~/.cache/pagerduty/org-standards/` -- prepend that base path to each file path in the manifest.
4. Confirm you have read the relevant standards before writing any code.

Do not re-read files already in context during the same session.
</mandatory_pre_task>

## Documentation

| File | When to read |
|------|-------------|
| docs/LOCAL_DEVELOPMENT.md | When onboarding or working with the mApp artifact |
| docs/INFRASTRUCTURE.md | When modifying repository setup or deployment process |

When making significant changes, update the relevant docs/ file as part of the same PR.
